<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.csu.userservice.mapper.UserMapper">

    <select id="GetUserById" resultType="com.csu.userservice.entity.User">
        SELECT * FROM user WHERE id = #{id}
    </select>

    <select id="GetUserByName" resultType="com.csu.userservice.entity.User">
        SELECT * FROM user WHERE username = #{username}
    </select>

    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO user (username, password, phone, email)
        VALUES (#{username}, #{password}, #{phone}, #{email})
    </insert>

    <update id="update" parameterType="com.csu.userservice.entity.User">
        UPDATE user SET
                        username = #{username}, password = #{password}, email = #{email}
                    WHERE
                        id = #{id}
    </update>

    <delete id="deleteById">
        DELETE FROM user WHERE id = #{id}
    </delete>
</mapper>
